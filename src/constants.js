// Danh sách danh mục món dùng cho filter (Menu) và form thêm/sửa món (Dashboard).
// Đây là cấu hình UI cố định, không phải dữ liệu nghiệp vụ — dữ liệu món/đơn hàng
// thật nằm hoàn toàn trên mockapi.io.
export const CATEGORIES = ["Coffee", "Tea", "Cold Brew", "Pastry"];
